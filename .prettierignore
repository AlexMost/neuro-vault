node_modules/
dist/

# subagent-driven-development scratch (ledger, task briefs, implementer
# reports, review diffs) — throwaway, deleted at the end of each apply run.
.superpowers/

# Vendored OpenSpec schema bundle (superpowers-bridge) — upstream-maintained,
# upgraded via rm -rf + cp -R; reformatting would diverge from upstream.
openspec/schemas/

# Generated opsx slash commands — regenerated by `openspec update`; reformatting
# would diverge from regeneration. The hand-written .claude/rules/ stay checked.
.claude/commands/

# Markdown is prose (docs, openspec artifacts, changelog) — prettier's wrapping
# and list reflow fight the authoring, so we don't gate it on a format check.
*.md
